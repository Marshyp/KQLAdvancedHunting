# Defender XDR detections

Each `.kql` file is a standalone Advanced Hunting query. Metadata is recorded in the header. Thresholds and allowlists are deliberately visible so teams can tune them. Coverage is summarized in [`../coverage.csv`](../coverage.csv).
